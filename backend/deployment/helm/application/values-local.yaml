# Local development overrides.
# Applied on top of values.yaml for local minikube installations.

image:
  # Allow using a local dev image.
  repository: "pilates"
  tag: "latest"
  pullPolicy: Never
